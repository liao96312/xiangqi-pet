# 3D 棋盘更新方案

> 状态：已完成 GPU/WebGL 技术闸门和可玩的 3D 原型；红兵 / 黑卒已按重甲步兵设计图精修到 v7，具备分段圆顶盔、分层甲胄、长枪、长盾、背旗、统一 CC0 PBR 表面和五动作，并通过 `.blend → GLB → useGLTF/useAnimations` 接入。当前仍是约 15k 三角面的游戏模型，不以概念图级离线雕刻为目标。执行清单见 [`todolist.md`](todolist.md#10-3d-棋盘更新-todo)。

## 1. 目标

把当前二维棋盘升级为参考视频中的固定斜俯视 3D 战场，同时保留现有象棋规则、AI、提示、悔棋、复盘、棋谱、桌宠小窗和外围界面。

首版必须做到：

- 棋局仍然是标准九路十线中国象棋，楚河汉界和九宫清楚可辨。
- 棋子变成有角色轮廓、底座和动作的 3D 单位，但任何时候都能认出棋种与阵营。
- 选择、合法落点、提示、最后一步、吃子、将军和绝杀都有对应的 3D 反馈。
- 3D 不改变 AI 搜索流程和棋力，也不阻塞 AI 思考。
- WebGL 不可用或性能不足时自动回到现有 2D 棋盘。
- Windows 安装包中的表现与开发环境一致。

## 2. 不做的内容

首版不做自由镜头、联网对战、物理引擎、地图编辑器、复杂后处理，也不制作 32 个互不复用的角色。以上内容只有在 3D 棋盘稳定后出现明确需求才考虑。

## 3. 复用策略

### 3.1 复用当前项目

- `src/components/Board.tsx` 的输入数据继续作为 3D 棋盘的输入契约，不建立第二套棋局状态。
- `src/hooks/useBoardAnimation.ts` 继续提供走子、落定、吃子、将军和绝杀事件；3D 层只消费事件，不重新判断规则。
- `src/game/`、Pikafish、悔棋、提示、复盘、棋谱和现有右侧对局台不改业务逻辑。
- 现有 2D 棋盘保留为兼容模式和故障降级，不删除。
- 将军、绝杀等文字提示继续使用现有 DOM 叠层，首版不在 3D 场景中重复实现文字系统。

### 3.2 复用成熟工具

运行时只新增以下 3D 依赖：

- [`three`](https://threejs.org/)：WebGL 渲染基础。
- [`@react-three/fiber`](https://r3f.docs.pmnd.rs/)：把 Three.js 场景接入现有 React，使用其原生指针事件完成点击拾取。
- [`@react-three/drei`](https://github.com/pmndrs/drei)：复用 `useGLTF`、`useAnimations`、模型预加载、相机和性能辅助组件。

不引入完整游戏引擎、物理引擎或新的全局状态库。

资产与离线工具：

- 原型角色优先使用 [Quaternius Ultimate Animated Character Pack](https://quaternius.com/packs/ultimatedanimatedcharacter.html)：CC0，可商用，包含动画和 Blender/FBX 源文件。
- 通用人形动作可选 [Mixamo](https://helpx.adobe.com/creative-cloud/faq/mixamo-faq.html)：可免版税用于商业项目，但 Adobe 明确说明中国区账号不可用，因此只能作为补充，不能成为唯一来源。
- 木材、石材、金属和 HDRI 使用 [Poly Haven](https://polyhaven.com/license) 或 [ambientCG](https://ambientcg.com/) 的 CC0 资产。
- 角色整理、骨骼重定向和动画裁剪使用 Blender 内置的 [glTF 2.0 导出器](https://docs.blender.org/manual/en/latest/addons/import_export/scene_gltf2.html)，统一导出 `.glb`。
- 发布前用 [glTF Transform](https://gltf-transform.dev/cli) 检查、去重和压缩资产。它只属于离线资产流程，不加入应用运行时。

每个进入仓库的外部资产都要在 `THIRD_PARTY_ASSETS.md` 记录来源、作者、许可证、下载日期和本项目修改内容。许可证不明确、禁止商用或要求禁止再分发的资产不使用。

## 4. 最小技术设计

### 4.1 渲染边界

`App` 仍然负责棋局和界面状态，只在棋盘位置选择 `Board3D` 或现有 `Board`：

```text
现有棋局状态 / useBoardAnimation
             |
             v
     相同的棋盘输入契约
        /            \
   Board3D         Board（现有 2D）
      |                 |
 WebGL 正常        手动选择或自动降级
```

3D 场景不得直接调用 AI、修改历史栈或复制走法判断。

### 4.2 棋盘与坐标

- 棋盘用基础几何体和线段程序生成，不下载通用西洋棋盘模型。
- 统一坐标转换：`x = col - 4`，`z = row - 4.5`；翻转只经过一个转换入口。
- 使用固定斜俯视正交相机，默认禁止旋转和缩放，优先保证九路十线的可读性。
- 90 个交叉点使用 R3F 指针事件触发已有 `onChoose(pos)`。
- Canvas 外保留语义化操作层，确保键盘和读屏能力不低于当前版本。

### 4.3 棋子方案

最终棋子采用“战场单位”方向，不做木制棋子，也不把圆柱、锥体等程序化几何当成最终美术。每次只完成一个棋种，先通过最大画面验收，再进入下一个：

`卒 / 兵 → 炮 → 车 → 马 → 象 → 士 → 将 / 帅`

识别优先级是角色轮廓、武器 / 载具、体量和动作；汉字、旗帜和阵营色只是辅助信息。去掉汉字和 UI 后，固定远景仍必须能看懂棋种。

七种角色的美术基准如下：

| 棋种 | 角色轮廓 | 主要识别物 |
|---|---|---|
| 将 / 帅 | 大型主将单位 | 冠、披风、主武器、军旗，体量最大 |
| 士 / 仕 | 近卫武士 | 护甲、刀剑或仪仗兵器 |
| 象 / 相 | 披甲战象 | 象头、长鼻、象牙、鞍甲 |
| 马 | 骑兵单位 | 马首、鬃毛、骑手或长枪 |
| 车 | 装甲战车 | 车轮、车厢、护板、旗帜 |
| 炮 | 炮兵单位 | 炮架、车轮、粗炮管、弹药结构 |
| 卒 / 兵 | 步兵单位 | 头盔、短兵器、盾牌或长枪，数量感明确 |

红黑双方复用模型，只替换朱砂红 / 墨黑材质、旗帜和少量配件。统一战场底座用于落点和阵营识别，但不能靠底座汉字掩盖角色轮廓不清的问题。

### 4.4 动画映射

统一使用五个基础动作名：`Idle`、`Move`、`Attack`、`Hit`、`Death`。

| 现有事件 | 3D 表现 |
|---|---|
| 选中 | 角色微抬、底座描边 |
| 合法落点 | 地面圆环 |
| 普通走子 | `Move` + 起点到终点插值 + 落地反馈 |
| 吃子 | 攻方 `Attack`，守方 `Hit` / `Death`，随后攻方落位 |
| 悔棋 | 使用现有反向移动事件回到起点，不倒放死亡动画 |
| 将军 | 保留现有弹字，叠加一次短促灯光或轻震屏 |
| 绝杀 | 保留现有弹字，增强主将受击和短暂定格 |

动画只改变显示，不延后棋局状态提交。被吃棋子由 `move.capture` 临时渲染到动作结束，避免另建战斗状态机。

### 4.5 GPU 与降级

当前 `electron/main.ts` 主动关闭了硬件加速、GPU 和 GPU sandbox，这是 3D 的前置阻塞项。实施时先查清这些开关的历史原因，再做最小调整：

1. 恢复 Electron 默认硬件加速并验证开发版。
2. 验证 NSIS 安装包、窗口透明、置顶、托盘和 Pikafish 同时运行。
3. Renderer 创建场景前检测 WebGL；失败时直接加载现有 2D 棋盘。
4. 设置中提供 2D / 3D 选择，自动降级后不循环重试。

不为首版编写 GPU 黑名单或自定义崩溃恢复框架；真实兼容问题出现后再补。

## 5. 性能与资产预算

- 固定镜头下优先维持流畅交互；开发机目标 60 FPS，低配机器不低于 30 FPS，否则降级 2D。
- 单个角色原型控制在约 15k 三角面以内，纹理优先 1024，避免为棋盘视角使用 4K 贴图。
- 相同 GLB 只加载一次；静态重复物使用实例化，带骨骼角色复用缓存的模型与材质。
- 首版使用一盏主光、环境光和低成本落地阴影；实时接触阴影、后处理和粒子仅在性能预算允许时启用。
- 发布前执行 `gltf-transform inspect`，只对确实存在的几何、纹理或 draw call 问题做优化。

## 6. 交付阶段

### 阶段 A：技术闸门

恢复 WebGL、完成 2D 自动降级，并用打包版证明 Electron 透明窗口、Pikafish 和 3D 能同时稳定运行。此阶段不做正式角色。

### 阶段 B：可玩的 3D 原型

程序生成棋盘，使用简单占位棋子，接通点击、翻转、全部高亮和普通走子。完成后，完整一局棋不需要切回 2D。

### 阶段 C：资产样板

只做一名步兵和一个主将，打通 Blender → GLB → React → 安装包全流程，并验证五个基础动作、材质、授权记录和包体。样板不通过就不批量制作其余棋种。

### 阶段 D：七种棋子与战斗反馈

完成七种轮廓、红黑变体和统一底座，接通走子、吃子、悔棋、将军与绝杀表现。

### 阶段 E：优化与发布

做低配测试、减少动态分辨率和动画、压缩资产，完成回归与安装包。先发布 `v1.3.0-alpha` 供体验；确认稳定后发布 `v1.3.0`，不为每个内部阶段单独发 Release。

## 7. 总体验收标准

- 标准象棋规则、AI 棋力、提示、悔棋、复盘和棋谱行为与 2D 版一致。
- 红黑视角下点击位置、合法落点、最后一步、提示与绝杀高亮全部正确。
- 普通走子、吃子、悔棋连续发生时没有重影、幽灵棋子或错误模型。
- 3D 资源全部有可核查许可证记录，安装包不从网络临时加载模型或纹理。
- `npm run selftest`、`npm run build`、`npm run benchmark:engine` 和 `npm run build:installer` 全部通过。
- WebGL 不可用时仍能进入 2D 棋盘并完整下棋。
- Windows 安装、升级、卸载和 GitHub Release 下载包均可正常使用。
