# 项目整理 TODO

这个项目目前的核心问题不是“功能不够”，而是：
- 结构有点散
- 页面信息太多
- 架构、视觉、文档都还有收敛空间

这份清单的目标，是让下一个 AI 能够按步骤推进，而不是只知道“要优化”，却不知道从哪里下手。

---

## 总原则

- 先减法，后加法
- 先主流程，后增强项
- 先清晰，后完整
- 先找最乱的地方，再逐步收敛
- 任何新功能都必须回答：它是在帮助用户更快使用，还是只是在让页面更满

---

## 0. 开工前先确认范围

### 目的
避免后面的 AI 误以为任务是“重做整个项目”，导致范围偏移。

### 需要先确认的事情
- [x] 先看一遍项目现状，明确它当前主要是“桌宠 / 对局辅助 / 信息展示 / 其他”中的哪一种
- [x] 先找出当前最核心的 1 条主流程，不要同时优化太多支线
- [x] 先判断当前最影响体验的到底是：功能太多、页面太挤、代码太乱、还是视觉太杂
- [x] 先决定本轮优先级，只做最影响体验和维护的部分

### 判断标准
- 如果改动会影响很多文件，但收益不明显，先不要做
- 如果某个模块已经明显拖慢理解和修改速度，优先处理它
- 如果一个问题只能靠大范围重构解决，就先做拆分，不要边修边加

---

## 1. 先做项目收敛

### 目标
让项目定位、核心功能、当前重点一眼可见。

### 需要做什么
- [x] 梳理现有功能，分成“核心功能 / 增强功能 / 可后置功能”
- [x] 合并重复表达的功能说明，删掉意思一样但说法不同的内容
- [x] 把 README 收敛成短、清楚、可执行的说明
- [x] 明确当前版本只保留 1 条主线，不要同时推进太多方向
- [x] 写清楚当前项目“最重要的下一步”
- [x] 明确哪些内容是给用户看的，哪些内容是给开发者看的，分开写，不要混在一起

### 建议怎么做
1. 先列出所有功能名称
2. 再标记每个功能属于核心、增强还是后置
3. 再删除那些“有但不常用、看着多但没必要”的内容
4. 最后再把说明文案压缩成简短版本

### 完成标准
- 打开项目后能快速知道它是做什么的
- 能快速知道现在最该优先做什么
- 不再有“功能很多但主线不清”的感觉
- 文档不会让人看半天还不知道先做什么

---

## 2. 前端信息层级收敛

### 目标
页面更轻、更清楚，主操作更突出。

### 需要做什么
- [x] 找出页面上最占空间、最干扰操作的信息区块
- [x] 把次要信息收进折叠区、侧栏或二级面板
- [x] 保留主操作区，只显示当前最需要的内容
- [x] 统一按钮、卡片、提示语的视觉层级
- [x] 区分桌宠小窗和主界面的信息密度上限
- [x] 检查有没有“默认全展开”的区域，能收起的先收起
- [x] 检查有没有重复出现的状态说明或提示文案，尽量合并

> 本轮已处理：按钮统一 transition/hover/active 反馈，按层级分品牌(38)/主图标(34)/次图标(30)/文字开关(30)四档；卡片统一 --radius-md 圆角，边框按重要性区分；提示语统一 --font-xs + --color-text-secondary。小窗(220×58)只保留状态+退出，主界面才展开详情面板。规范写入 DESIGN.md「信息层级与密度」章节。

> 2026-07 前端重设计：默认桌面窗改成“棋盘主舞台 + 右侧对局台”，上下棋手条显示身份、行棋状态与吃子记录；六个高频操作集中为 2×3 动作区，难度/执子/自动陪练降为设置项；评分、分析、复盘和棋谱统一进入底部详情抽屉。500px 以下切换单列并允许纵向滚动，默认窗口高度由 860 收敛为 620。

### 建议怎么做
1. 先观察页面里哪些内容是用户进入后第一眼必须看到的
2. 再把不属于第一眼内容的区域降权
3. 如果某块内容只在少数场景下用到，就不要默认展示
4. 如果同类信息有多个入口，尽量保留一个主入口，其余做辅助入口

### 完成标准
- 用户不用扫一大堆内容就能开始操作
- 页面看起来更干净，不再“挤满一屏”
- 主棋盘 / 主操作区域更突出
- 辅助信息不会抢主流程注意力

---

## 3. 架构还有优化空间

### 结论
有。现在更需要做的是“减复杂度、降耦合、拆职责”，而不是继续加功能。

### 先判断是不是架构问题
如果出现下面这些情况，说明架构值得优化：
- 改一个小功能，要改很多地方
- 很多逻辑都堆在一个组件里
- 页面逻辑和数据逻辑混在一起
- 状态经常互相影响，改动容易出副作用
- 代码能跑，但不好读、不好拆、不好扩展

### 重点检查项
- [x] 功能模块是否职责过大，一个文件/组件承担太多事情
- [x] 状态管理是否过于集中或过于分散，是否存在重复同步
- [x] UI 层、业务层、数据层是否混在一起
- [x] 公共逻辑是否散落各处，导致维护成本高
- [x] 页面是否把“展示、交互、状态、计算”都写在同一层
- [x] 是否存在历史包袱代码，已经不用但还在拖累结构
- [x] 是否存在“看似复用，实际上耦合更重”的抽象

> 本轮已处理：
> - 删除 `useXiangqiGame.ts` 私有副本（chooseAiMove/findMatchingLegalMove/sameMove/sameSquare），统一用 `moveUtils.ts` 和 `ai.ts` 导出
> - 删除 `useBoardAnimation.ts` 私有 sameMove，统一用 moveUtils
> - 删除 `ai.ts` 循环依赖 import（从 useXiangqiGame 导入 DifficultyKey）
> - 修复项目此前 build 失败（16 个 TS 错误：import 冲突 + 缺失 getLegalMoves 导入）
> - xiangqi.ts/fen.ts 内的死导出（moveToUci/pieceFen/pieceLabel/cloneBoard）因属"不要动"区域，保留不动

### 可执行优化方向
- [x] 按职责拆分模块，把大组件拆小
- [x] 把通用逻辑抽到独立层，避免重复实现
- [x] 统一状态流转方式，减少隐式联动
- [x] 梳理目录结构，让页面、组件、服务、工具各归其位
- [x] 把高频变动区和稳定基础能力分开
- [x] 对复杂页面做主流程优先重构，只保留必要链路
- [x] 如果一个功能可以通过更简单的方式实现，就优先选更简单的方案

> 本轮已处理：
> - 评估当前 `components/ + game/ + hooks/ + books/` 分层已清晰，强行再拆属"为拆而拆"，改为在 README 补「目录职责」说明，明确标注稳定基础(xiangqi/fen/notation/moveUtils/bookGuide) vs 高频变动(useXiangqiGame/ai/components/hooks/styles.css)
> - 状态流转已统一：useXiangqiGame 作为唯一业务编排层，positionIdRef 统一取消异步任务，components 纯展示
> - styles.css 1099 行评估后不拆分（CSS 声明式、按区块组织、Vite 已 minify），改为 token 化统一视觉语言

### 建议的拆分顺序
1. 先找最复杂、最常改、最难看懂的模块
2. 再找依赖最多、影响范围最大的模块
3. 先拆大块，再整理小块
4. 先保证职责清楚，再考虑进一步抽象

### 完成标准
- 改一个功能时，不需要全局联动很多地方
- 代码结构更容易理解和扩展
- 页面和逻辑都更轻，后续迭代风险更低
- 下一个 AI 不需要看一堆文件才能知道改哪里

---

## 4. 美术与视觉表现收敛

### 目标
让项目看起来像一个统一产品，而不是拼接出来的界面。目标不是“花”，而是“有气质、有压迫感、有辨识度”，同时保证操作清晰。

### 先判断是不是视觉问题
如果有这些现象，就说明视觉需要统一：
- 不同页面看起来像不同产品
- 图标、贴图、按钮风格混杂
- 动画太多，影响操作效率
- 颜色和字号没有统一规则
- 界面看起来“很满”，但实际重点不突出
- 棋盘、棋子、技能提示、杀法弹字没有形成同一种气质

### 视觉总方向
本项目的视觉应该偏向：
- 中国象棋的“古典感 + 冷峻感 + 竞技感”
- 不是幼态卡通风
- 不是廉价手游风
- 不是花哨堆特效
- 而是“克制、锐利、稳、霸气、干净”

### 棋盘美术指导
- [x] 棋盘要有明确的层次：底板、木纹/石纹纹理、格线、边框、阴影要分层
- [x] 纹理不要太抢眼，重点是衬托棋子，不是喧宾夺主
- [x] 线条要干净，格线不要虚、不要飘、不要太细导致看不清
- [x] 边角可以有一点点传统器物感，但不要太复古杂乱
- [x] 整体色调建议沉稳，偏深木色、墨色、暗红或青铜感点缀
- [x] 棋盘中心和边框可以留出一点“呼吸空间”，不要把元素塞满
- [x] 坐标、河界、楚河汉界如果保留，必须和整体风格一致，不要像后加上去的

> 已处理：纹理从条纹改成自然木纹节点（radial-gradient 椭圆叠加），不抢眼；边框加青铜器物感双层内嵌线（rgba(107,74,44) + rgba(199,162,90) 高光）；格线 stroke-width:2 干净不虚；兵卒点标记 stroke-width 1.5→1.8 更清晰；::before inset 8px→10px 加呼吸空间；河界文字 fill 0.26→0.34 提色但保持克制；底色用 design.md 棋盘色板 token。

### 棋子美术指导
- [x] 棋子必须一眼高级，不能像普通扁平圆形按钮
- [x] 棋子建议有明显的体积感、边缘高光、暗部过渡和落地阴影
- [x] 棋子外形要有统一家族感，所有棋子看起来是同一套工艺做出来的
- [x] 字体建议做定制化处理，不能直接用系统默认字形就完事
- [x] 字要清晰、硬朗、有力量感，局部可以有金属、玉石、木雕、篆刻等气质，但不能混乱
- [x] 红黑两方要有强对比，但不要俗气大红大黑，最好是“朱砂红 / 墨黑 / 金属灰”这类更耐看的组合
- [x] 高亮状态、选中状态、可走状态，要通过光晕、描边、轻微浮起表达，不要靠很重的闪烁
- [x] 棋子移动时要有“起、行、落”的节奏感，像真正落子，而不是滑一个圆片

> 已处理：配色修正——红方改用 design.md token --color-red(#b64235 朱砂红)，黑方改用 --color-black(#2f2b27 墨黑)，不再硬编码偏离规范；字体加刀锋篆刻感（text-shadow 4 层：底层暗影 rgba(40,24,12,0.35) + 中层描边 currentColor + 上层高光 rgba(255,239,178,0.42)）；边缘轮廓 border 改用 --color-board-border 加深避免与棋盘融合；选中态加微抬升（selected-piece-pulse keyframe 加 translateY(-2px)）+ 外圈能量感（filter: drop-shadow 蓝色光晕，符合 design.md 状态色分工）。体积感/家族感/移动节奏已具备（radial-gradient + moving-piece-pop 起行落节奏）。

### 棋子质感建议
- [x] 可以考虑“半写实 + 轻拟物”的方向，不要纯扁平
- [x] 表面可以有轻微磨砂、釉面、木质、玉石、金属中一种主质感
- [x] 质感要统一，不要每个棋子像不同材质
- [x] 棋子边缘要有柔和但明确的轮廓，避免和棋盘融在一起
- [x] 选中时可增加轻微发光、外圈能量感、微抬升和柔和阴影

> 已处理：半写实轻拟物（radial-gradient 体积感 + 多层 inset/outset box-shadow 边缘高光暗部过渡）；主质感统一为木质釉面（#f5d795→#dda85e→#a56d38 径向渐变模拟木釉）；边缘轮廓 border 用 --color-board-border(#6b4a2c 青铜木色) 加深，明确不融合；选中态 filter:drop-shadow 蓝色发光 + translateY(-2px) 微抬升 + selected-piece-pulse 柔和脉冲。符合 design.md「棋子质感」规范。

### 动画要求
- [x] 动画必须流畅，不能卡顿、拖沓、抖动
- [x] 走子动画要有加速度和减速度，不要匀速机械滑动
- [x] 落子时建议有轻微弹性回弹，但幅度要克制
- [x] 吃子、将军、绝杀这类动作要有更强反馈，但仍然保持高级感，不要廉价爆炸特效
- [x] 动画时间建议短而明确，用户能感到“爽”，但不会等太久
- [x] 不要所有动作都上大特效，只有关键节点才增强表现
- [x] 动画与音效如果有，要同步节奏，形成“落子感”

> 已核对：所有动画时长完全符合 design.md 动画时长表——走子 0.22s cubic-bezier(0.16,0.88,0.34,1) 带末端减速、落子 0.28s cubic-bezier(...,1.1) 轻微过冲、吃子火花 0.42s、将军 0.82s、绝杀 1.02s、横幅 1.45s。普通走子只有 moving-piece-pop(0.26s) 轻反馈，只有吃子(capture-spark+cell-shake)/将军(check-flash-pop)/绝杀(mate-flash-pop)才有强反馈。第 7 项音效同步：项目当前无音效系统，不适用，保留架构预留。prefers-reduced-motion 已覆盖所有循环动画（选中脉冲/无保护呼吸/悬空呼吸/弹字）。

### 杀法/将军/绝杀弹字指导
这是重点，必须做得炫酷、霸气、但不能俗。

#### 文案气质
- [x] 字体要有力量感，优先考虑带棱角、压迫感、攻击性的字形
- [x] “将军”“绝杀”“一招制胜”“破局”“必杀”等字要像战报，不像普通提示
- [x] 字体笔画可以有轻微金属感、刀锋感、火焰边缘感、能量裂纹感，但不要堆太多元素
- [x] 不要用过于幼态、可爱、Q版的字体风格

> 已处理：字体 KaiTi/STKaiti 书法体 + font-weight:900 极重，有棱角压迫感；text-shadow 多层模拟刀锋篆刻感（底层暗影+中层描边+上层高光）；将军弹字朱砂重 #a11214 + 墨黑阴影压底 rgba(20,12,6,0.55)，绝杀弹字加金色刀锋描边 rgba(183,120,37,0.85)（design.md --color-gold）模拟金属边缘。

#### 出场方式
- [x] 弹字不要只是淡入，建议有“快速冲入 + 停顿定格 + 轻微震动”
- [x] 可以从屏幕中心偏上或棋盘关键位置炸出，形成视觉冲击
- [x] 字体出现时要有层次：底层阴影 / 中层主体 / 上层高光
- [x] 可以有一瞬间的描边闪亮，增强“判定成立”的感觉
- [x] 弹字结束后要迅速收束，不要停留太久遮挡棋盘

> 已处理：check-flash-pop/mate-flash-pop keyframe 实现「冲入(scale 0.4→1.18)+定格(30%回弹)+震动(24%-52% 间 translateX ±3-4px 抖动)+收束(100% scale 放大消失)」；层次分明（text-shadow 底层墨黑阴影+中层 currentColor 描边+上层暖色高光）；绝杀金色描边闪亮增强判定感；收束在 62%-100% 快速消失不遮挡。

#### 视觉风格建议
- [x] 绝杀类字样可以更偏金色、红金、暗红金属、青白电光等高压配色
- [x] 将军类字样可以偏红色冲击感，带一点墨黑阴影压住
- [x] 高级一点的做法是“重描边 + 内渐变 + 微粒子 + 轻震屏”，不要单纯发光
- [x] 边缘效果要精致，宁可少一点，也不要廉价大闪光
- [x] 字体最好有定制感，像专门为这个项目设计的战斗提示字

> 已处理：将军弹字 #a11214 朱砂重 + 墨黑阴影压底（rgba(20,12,6,0.55) 4px 偏移 + 6px 模糊）；绝杀弹字 #a11214 朱砂极重 + 金色描边（rgba(183,120,37,0.85) design.md --color-gold）+ 暗红金属边缘感 + 更强震动；重描边（多层 text-shadow）+ 轻震屏（keyframe translateX 抖动）；边缘精致不用大闪光（text-shadow 最多 20px 外发光，克制）；字体 KaiTi 书法体 weight 900 有定制战报感。配色以 design.md 状态色分工为准（将军/绝杀均 #a11214 朱砂重/极重，区分靠字号+收束速度+金色描边）。

### 推荐风格关键词
如果要让后续 AI 继续细化，可以直接朝这些方向做：
- 中国风
- 竞技感
- 霸气
- 冷峻
- 高级
- 克制
- 压迫感
- 金属感
- 书法刀锋感
- 战报感
- 玉石 / 金属 / 木器质感

### 重点检查项
- [x] UI 风格是否统一
- [x] 图标风格是否一致
- [x] 贴图质量是否稳定、清晰度是否一致
- [x] 动画是否过多、过慢、过花
- [x] 颜色系统是否清楚，主色 / 强调色 / 状态色是否分工明确
- [x] 字体、间距、圆角、阴影是否形成统一视觉语言
- [x] 小窗、主界面、设置页是否遵循同一套视觉规则
- [x] 棋子和棋盘是否彼此衬托，而不是互相抢戏
- [x] 杀法弹字是否够霸气，但又不廉价

### 可执行优化方向
- [x] 先定一套统一视觉规范，再做局部美化
- [x] 图标统一风格和线条粗细
- [x] 贴图优先清晰、干净、比例一致
- [x] 动画只服务反馈和引导，不抢操作本身的注意力
- [x] 主界面保持克制，减少装饰性元素，突出棋盘和操作区
- [x] 小窗模式优先保证识别度和可读性
- [x] 所有视觉资源尽量沿用同一主题气质，不要混搭太多风格
- [x] 棋子、棋盘、弹字要形成统一审美体系，不要各做各的

> 本轮已处理：棋盘木纹纹理+青铜边框器物感，棋子朱砂红/墨黑 token 配色+刀锋篆刻字体+选中微抬升蓝光晕，弹字朱砂重+墨黑压底（将军）/金色刀锋描边（绝杀）+震动关键帧，全部用 design.md 定义的色板/字体/动画时长。棋子棋盘互相衬托（棋子边缘 --color-board-border 加深不融合，棋盘纹理不抢眼），弹字霸气但克制（最多 20px 外发光，重描边而非大闪光）。统一审美体系：朱砂红/墨黑/青铜木色/金 + KaiTi 书法体贯穿棋子弹字。

> 本轮已处理（第三阶段美术打磨）：
> - 棋盘：木纹节点纹理 + 青铜器物感双层内嵌边框 + 兵卒点标记加粗 + 河界文字提色 + 呼吸空间加大
> - 棋子：配色修正用 design.md token（朱砂红#b64235/墨黑#2f2b27）+ 刀锋篆刻 text-shadow 4 层 + 边缘轮廓 --color-board-border 加深 + 选中态微抬升 translateY + 蓝光晕 drop-shadow
> - 弹字：将军朱砂重#6412e+墨黑阴影压底+刀锋描边；绝杀加金色描边(--color-gold)+暗红金属感+独立 mate-flash-pop 震动关键帧；层次分明（底层阴影/中层主体/上层高光）
> - 动画：时长全符合 design.md 表格；prefers-reduced-motion 补全选中脉冲/无保护呼吸/悬空呼吸覆盖
> - 全部以 design.md 状态色分工/色板/字体/动画时长为准，未偏离规范

> 2026-07 视觉重设计：参考成熟在线棋类产品的“棋盘优先、少量高频操作常驻、分析信息降级”模式，外壳改为深墨竞技界面；棋子由红黑整块高光材质改为同一套暖木棋坯，以朱砂字/墨字区分阵营，和木棋盘保持一致材质语言；保留现有走子、吃子、将军、绝杀反馈。

### 完成标准
- 视觉上是同一个产品，而不是多个素材拼起来的
- 图标、贴图、动效有统一语言
- 棋盘有质感，棋子有高级感，弹字有冲击力
- 界面更简洁、更耐看、更专业
- 用户能一眼看出哪里是重点

---

## 5. 文档和交接信息整理

### 目标
让后面的 AI 或开发者不需要猜，能直接接手。

### 需要做什么
- [x] README 只保留最重要的信息，避免长篇堆叠
- [x] 增加“当前项目状态”和“下一步建议”
- [x] 把容易误解的地方单独说明清楚
- [x] 写清楚哪些内容暂时不要动，避免后续 AI 误改
- [x] 如果有构建、启动、发布相关内容，要写成最短路径说明

### 建议怎么写
- 项目是干什么的
- 当前最重要的问题是什么
- 现在应该先改哪里
- 哪些地方是核心，哪些地方只是增强
- 哪些地方改之前要先确认

### 完成标准
- 新接手的人不需要问太多就能开始
- 不容易把精力花在次要问题上
- 不容易因为目标不清而偏移

---

## 6. 推荐执行顺序

### 第一阶段：先收敛
- [x] 清理项目说明和功能分类
- [x] 收敛 README
- [x] 做一次架构体检，找出最臃肿的模块和最难维护的链路
- [x] 定一套基础视觉规范

### 第二阶段：再减负
- [x] 重排前端信息层级
- [x] 收起次要模块
- [x] 统一样式体系
- [x] 按职责拆分高耦合模块

### 第三阶段：再增强
- [x] 确认下一项增强功能为 3D 棋盘，并完成复用调研与实施方案
- [x] 再做细节打磨
- [x] 再补性能和体验优化
- [x] 已按第 10、11 章推进 3D 棋盘主线并完成七类正式静态模型接入；剩余动作、性能、安装包与发布事项统一以第 12 章为准

> 第一、二阶段已完成。第三阶段已确定只推进 3D 棋盘；详细方案见 `3D_UPDATE_PLAN.md`，其他增强功能继续后置。

---

## 7. 当前最重要的下一步

当前唯一开发主线仍是 3D 棋盘。GPU / WebGL 技术闸门、全屏镜头以及七类红黑静态模型已经完成；当前下一项改为“棋盘落面与统一朝向”，随后依次推进“完整对局回归 → 骨骼与棋种动作 → LOD / 性能实测 → 安装包回归 → Release”。资产授权清单作为并行发布闸门持续补齐，不阻塞本地视觉重构。

第 10、11 章保留为历史实施记录；从 2026-08-12 起，只按第 12 章的当前执行清单逐项推进。

---

## 8. 已发现的 bug 风险与优先排查项

> 说明：下面这些不是“百分百已坏”的结论，而是从当前代码审查里看到的高风险点。后续 AI 应先验证，再决定修复顺序。

### 8.1 AI 思考和提示状态可能出现不同步

#### 风险描述
- `showHint()`、`makeAiMove()`、引擎分析都依赖异步延迟和当前局面
- 如果用户在 AI 延迟期间快速点操作，虽然有部分状态校验，但仍存在提示、思考状态、棋盘状态短暂不同步的风险

#### 需要验证
- [x] 连续快速点击棋盘、悔棋、提示、换边时，AI 是否会落到旧局面
- [x] `thinking` 状态是否有机会卡住不恢复
- [x] `hint` 是否会显示过期走法

#### 建议处理
- [x] 给所有异步 AI 行为加更严格的局面版本号校验
- [x] 在新局面开始时统一取消旧任务
- [x] 对提示、分析、走子共享同一套“当前局面令牌”

> 已处理：在 `useXiangqiGame.ts` 中引入 `positionIdRef`，`showHint` 和 `makeAiMove` 在异步回调前校验当前局面版本号，若已变化则直接取消。

### 8.2 悔棋逻辑在自动陪练场景下可能存在边界问题

#### 风险描述
- `undo()` 依赖历史栈长度、当前轮次和最后一步棋子归属来判断回退 1 步还是 2 步
- 当自动陪练开启、切换执棋方、或者 AI 正在延迟出手时，回退步数可能出现边界误判

#### 需要验证
- [x] 开启自动陪练后，连续悔棋是否总能回到预期局面
- [x] 切换红黑方后再悔棋，是否会多退或少退
- [x] AI 未完成出手时悔棋，历史栈是否稳定

#### 建议处理
- [x] 把“人类一步 / AI 一步”显式记录成回合单位
- [x] 悔棋时按回合回退，而不是临时推断回退几步
- [x] 对自动陪练和手动对弈分别走独立回退路径

> 已处理：新增 `src/game/undo.ts`，为每个历史节点记录 `player/ai` 操作者；自动陪练按完整“玩家+AI”回合回退，AI 尚未应答时只回退玩家一步，开局由 AI 先行时也只回退 AI 一步；手动对弈始终只回退最近一步。相关边界已加入 selftest。

### 8.3 复盘结果依赖强引擎，可能在引擎不可用时给出弱反馈

#### 风险描述
- `runReview()` 只有在 `playAnalyze` 可用时才真正进行复盘
- 如果引擎不可用，UI 只会显示“复盘不可用”，但用户可能误以为功能坏了

#### 需要验证
- [x] 引擎缺失时，复盘提示是否足够明确
- [x] 引擎分析超时、忙碌、报错时，是否有清晰降级说明

#### 建议处理
- [x] 给复盘按钮增加更明确的不可用原因
- [x] 区分“引擎没安装”“引擎忙”“引擎报错”三种状态
- [x] 增加无引擎时的轻量级本地复盘提示

> 已处理：useReview 区分「没走子/引擎没安装/引擎桥接未就绪/引擎忙/超时/未运行/其他报错」六种状态，给出对应文案。复盘按钮在 !engineAvailable 时 disabled，title 显示「复盘需安装 Pikafish 引擎」。EvalBar/AnalysisPanel 区分「未安装强引擎/引擎分析中.../正常」三态。无引擎时仍可对弈（内置陪练），只是复盘不可用。

### 8.4 部分杀法名称/判定可能有重名或误判风险

#### 风险描述
- `getCheckmateTactic()` 里有多种杀法判定，且部分判断条件比较接近
- 某些局面可能同时满足多个模式，导致展示的杀法名称不一定是最符合直觉的那个

#### 需要验证
- [x] 同一局面是否会出现杀法名称不稳定
- [x] 先后顺序是否会导致“更准确的杀法”被覆盖掉
- [x] 有没有一些极少见局面被误判成通用“绝杀”

#### 建议处理
- [x] 给杀法判定加明确优先级
- [x] 为每个杀法补覆盖测试
- [x] 如果多个条件同时成立，优先展示最典型、最霸气、最容易被用户理解的名称

> 已验证（selftest 覆盖 19 个杀法用例）：
> - 名称稳定：getCheckmateTactic 是纯函数无随机性，同局面多次调用结果一致（tacticNameIsStable 测试）
> - 非绝杀不误判：初始局面返回 null（nonMatePositionReturnsNull 测试）
> - 优先级已明确：判定顺序为 吃将 > 将帅照面 > 困毙 > 海底捞月 > 天地炮 > 马后炮 > 重炮 > 铁门栓 > 兜底绝杀
> - **已知限制（xiangqi.ts 属"不要动"区域，暂不修）**：天地炮的 hasCenterCannon 条件过宽（炮在将同列即满足），会拦截本应是"重炮"的局面；卧槽马/钓鱼马/双马饮/八角马/角马/侧面虎/车马冷着等判定函数已定义但未接入 getCheckmateTactic，会兜底成"绝杀"

### 8.5 盘面高亮和动画在快速连招时可能出现视觉残影

#### 风险描述
- 走子、吃子、将军、绝杀都有各自的动画和定时器
- 快速连续走子时，旧动画未完全结束就开始新动画，可能出现视觉重叠、闪烁或残影

#### 需要验证
- [x] 连续快速走子时，移动动画是否叠加
- [x] 吃子特效是否会残留
- [x] 将军/绝杀弹字是否会同时出现多个

#### 建议处理
- [x] 新走子发生时清理上一轮相关动画
- [x] 为动画引入统一队列或互斥机制
- [x] 对高优先级提示使用“只保留最后一次”的策略

> 已验证并修复：
> - moveAnimation/settleAnimation/captureBurst 用 key 控制 React 重挂载，每次 history 变化设置新值 + cleanup 清理定时器，无叠加残影
> - checkFlash/mateFlash 此前在条件 true→false 时 cleanup 只清定时器不清状态，导致 DOM 节点残留（CSS 动画 both 模式视觉会消失，但 DOM 不清理）。已修复：条件不满足时显式 setCheckFlashKey('')/setMateFlash(null)
> - 将军/绝杀弹字用 key 控制，同时只显示一个

### 8.6 棋盘视角翻转后，需要验证坐标和高亮是否完全一致

#### 风险描述
- 盘面支持翻转，但棋子位置、坐标标签、线条标记、高亮提示都依赖不同渲染逻辑
- 如果某个高亮层没有跟着翻转，用户会感觉“点的位置和显示不一致”

#### 需要验证
- [x] 翻转后点击位置是否与实际棋子一一对应
- [x] 走法提示、最后一步、高亮目标是否都随翻转正确显示
- [x] 小窗模式和大窗模式下翻转是否表现一致

#### 建议处理
- [x] 翻转逻辑只保留一个统一的坐标变换入口
- [x] 对所有叠层统一使用同一套 displayPos 计算
- [x] 给翻转模式补一次专项回归测试

> 已验证：Board.tsx 的 displayPos 统一了所有棋子、高亮、走法提示、最后一步的坐标变换，翻转后全部跟随。BoardLines 坐标标签翻转，棋盘线/标记对称不翻转（正确）。小窗模式不显示棋盘，不受翻转影响。河界文字"楚河/汉界"不随翻转换位属设计选择（文字方向不变），不影响功能。

### 8.7 AI 在不降低棋力的前提下降低延迟

#### 当前基线
- 当前机器为 6 核 12 线程，但分析引擎和对弈引擎各开 8 线程，AI 回合可能发生线程争抢
- 对弈引擎使用 `MultiPV 5`，主线搜索算力被五条候选线摊薄
- 当前安装包只带 `sse41-popcnt` 引擎，而目标机器支持 AVX2
- 单局面基准：当前 `8线程 / MultiPV 5 / 2200ms` 深度 17；`6线程 / MultiPV 2 / 1400ms` 深度 19

#### 待办
- [x] 安装包同时提供 AVX2 和 SSE4.1 构建，运行时优先 AVX2，启动失败自动回退 SSE4.1
- [x] 分开配置两个引擎：局面分析使用 2 线程，对弈引擎使用 6 个物理核
- [x] AI 思考期间暂停 450ms 局面分析，避免两个引擎争抢 CPU
- [x] 对弈引擎改为 `MultiPV 2`，保留最佳走法和防重复备选
- [x] 应用启动后后台预热对弈引擎，消除首步约 0.5 秒冷启动
- [x] 用 6 组开中局做深度回归；AVX2 新配置平均 1400ms / 深度 18.83，优于旧配置 2201ms / 深度 17.83，强模式已下调到 1400ms

#### 完成标准
- 强模式平均响应时间不高于 1.7 秒
- 相同测试局面的搜索深度、节点预算和最佳走法质量不低于当前版本
- 老 CPU 自动回退 SSE4.1，不因 AVX2 缺失而启动失败

> 回归命令：`npm run benchmark:engine`。该命令会比较旧版 `8线程 / MultiPV 5 / 2200ms` 和新版 `6线程 / MultiPV 2 / 1400ms`，新版平均深度低于基线或平均延迟超过 1700ms 时直接失败。

---

## 9. 给下一个 AI 的执行说明

- 先读完本文件，再看代码结构
- 优先找“最臃肿、最耦合、最难改”的地方
- 不要一上来加新功能，先做减法
- 所有修改都要围绕“更清晰、更轻、更好维护”
- 如果发现现有结构已经影响开发效率，优先重构而不是修补
- 如果某一项不确定怎么下手，先拆问题，再改代码，不要直接大改
- 如果任务范围开始变大，先回到本清单，重新确认优先级

---

## 10. 3D 棋盘更新 TODO

> 详细方案：`3D_UPDATE_PLAN.md`。严格按顺序执行；前一阶段没有通过验收，不开始下一阶段。

### 10.0 方案与复用调研

- [x] 分析参考视频的固定斜俯视镜头、角色棋子、移动、攻击、受击和死亡表现
- [x] 核对当前 `Board` 输入、坐标翻转与 `useBoardAnimation` 事件，确认不复制棋局状态
- [x] 选择 Three.js + React Three Fiber + Drei，不引入完整游戏引擎或物理引擎
- [x] 筛选许可证明确的角色、动画、材质和 HDRI 来源
- [x] 写完 `3D_UPDATE_PLAN.md`

### 10.1 阶段 A：GPU / WebGL 技术闸门

- [x] 从 Git 历史确认 `disableHardwareAcceleration`、`disable-gpu`、`disable-gpu-sandbox` 的加入原因和原始故障
- [x] 恢复 Electron 默认硬件加速，仅保留实际需要的兼容处理
- [x] 增加 WebGL 可用性检测；创建失败时自动使用现有 2D 棋盘
- [x] 增加 2D / 3D 显示设置并持久化，自动降级后不反复重试
- [x] 用最小 Three.js 场景验证开发版 3D 渲染、透明页面和基础窗口运行
- [x] 验证 Pikafish 进程与最小 3D 场景同时运行时没有明显卡顿或崩溃
- [x] 运行 `npm run selftest`、`npm run build` 和 `npm run build:installer`
- 安装版启动、对局与退出烟雾测试已迁移至第 12.5 节。

#### 阶段 A 完成标准

- 开发版 WebGL 已验证；安装版 WebGL 验收已迁移至第 12.5 节。
- [x] WebGL 不可用时仍能进入 2D 模式完整下棋（检测与自动降级已实现）
- [x] GPU 调整不影响 Pikafish、开发版 3D 页面和打包版进程启动

### 10.2 阶段 B：可玩的 3D 棋盘原型

- [x] 只新增 `three`、`@react-three/fiber`、`@react-three/drei` 三项运行时依赖
- [x] 新建最小 `Board3D`，与现有 `Board` 接受相同棋盘数据和 `onChoose(pos)`
- [x] 用基础几何体生成九路十线、九宫和楚河汉界，不导入西洋棋盘模型
- [x] 使用固定斜俯视正交相机，默认禁止自由旋转和缩放
- [x] 建立唯一的棋盘坐标转换，验证红黑视角翻转
- [x] 为 90 个交叉点接通点击和语义化键盘操作
- [x] 用简单占位棋子渲染完整初始局面
- [x] 接通选中、合法落点、最后一步、吃子目标、AI 提示和绝杀走法高亮
- [x] 接通现有普通走子、落定与悔棋动画事件
- [x] 保留现有 DOM 将军 / 绝杀弹字和 2D 棋盘降级路径

#### 阶段 B 完成标准

- 完整对局、双视角交互和快速操作回归已迁移至第 12.2 节。

### 10.3 阶段 C：资产样板

- [x] 新建 `THIRD_PARTY_ASSETS.md`，记录来源、作者、许可证、日期和修改内容
- [x] 用本项目自研程序化低多边形几何完成七种棋子轮廓样板，作为无外部资产的离线 fallback（仅技术占位，不作为最终美术）
- [x] 确定战场单位美术方向：靠角色轮廓、武器、载具和体量识别棋种，汉字只做辅助，不以木制棋子或抽象几何为最终形态
- [x] 从可核查授权的角色、武器、坐骑和载具素材中选可复用部件；不直接复制授权不明的 GitHub 模型
- [x] 先只制作一枚“卒 / 兵”作为美术基准，完成第一版比例、PBR 材质、骨骼、动作、最大画面构图和 GLB 导出
- [x] 生成卒 / 兵概念设计板：正面、侧面、三分之四视角和远景剪影，保存为 `art/concepts/pawn-infantry-v1.png`
- [x] 补齐红兵 / 黑卒成对概念板：复用同一套轮廓和装备，只切换阵营材质、旗帜与少量配件，保存为 `art/concepts/pawn-infantry-red-black-v2.png`
- [x] 概念评审通过后锁定卒 / 兵的比例、装备、材质和剪影，不在建模阶段临时改方向
- [x] 完成卒 / 兵灰模 blockout：头盔、躯干、肩甲、盾牌、长枪和旗杆，并在最大画面做远景剪影验证
- [x] 完成“卒 / 兵”第一版战场步兵视觉样板：头盔、躯干、盾牌、长枪和旗帜；程序化版本只保留为技术 fallback
- [x] 安装 Blender 5.2.0 LTS 便携版，固定到 `D:/Tools/Blender`
- [x] 用 Blender 生成红兵 / 黑卒 blockout `.blend` 与双 `.glb`，完成最大画面预览验收
- [x] 将红兵 / 黑卒 GLB 放入 `public/assets`，用 `useGLTF` + preload 接入 3D 棋盘
- [x] 保存用户确认的重甲步兵设计图到 `art/references/pawn-infantry-target.png`，作为后续建模对照
- [x] 重建卒 / 兵 v3 人形低模：独立头脸、四肢、手脚、胸甲、甲裙、肩甲、护臂护腿、长枪、长盾和背旗
- [x] 压暗钢材、皮革、皮肤和阵营色，缩窄并侧移盾牌，避免最大画面遮住人体与胸甲
- [x] 将 v3 带动画 GLB 接入棋盘，并用 Electron 1280×720 实际截图验证比例、遮挡和远景识别
- [x] 调研并采用 Blender Studio CC0 写实男性底模，替换 v3 的基础几何人体；保留 v3 作为离线回退
- [x] 用底模表面复制贴体胸甲、护臂、胫甲、靴子和头盔壳，减少方块拼接感
- [x] 采用 Poly Haven `Metal Plate 02` 的 CC0 1K OpenGL 法线贴图，为钢甲、暗钢和青铜增加磨损起伏
- [x] 生成红兵 / 黑卒 v4 GLB、Blender 工程和 1000×700 双阵营预览，盾面分别刻“兵 / 卒”
- [x] 对照重甲步兵效果图制作 v5：烘焙持枪持盾姿势，枪杆对齐左手，右手位于盾后握把
- [x] v5 重做内外层胸甲、V 形铜饰、叠片肩甲、缩窄外移盾牌、曲线盔缨和斜背破损旗
- [x] 为自制甲胄自动展开 UV，弃用带工业板缝的角色金属映射，改用 ambientCG Metal 010 的 CC0 细拉丝法线/粗糙度/金属度
- [x] 完成 v5 正面三分之四和侧视穿模检查；红兵 14,896 三角面，仍低于 15k 单角色预算
- [x] v6 从写实人体表面提取并加厚胸甲、头盔、肩甲、护臂、胫甲和战靴，替换 v5 的积木式方块 / 圆柱拼接
- [x] v6 裁掉自动权重拉坏的手指与前臂，改为贴甲锥形护臂和握拳；补齐侧甲裙并收紧围巾、盔环和盾牌比例
- [x] 完成 v6 正面三分之四与侧视穿模检查；红兵 14,077 三角面，仍低于 15k 单角色预算
- [x] 在 1920×1080 最大棋盘画面验收 v6：远景兵种轮廓、盾牌遮挡、PBR 材质与离线加载通过；脸部按近景资产保留，不作为几十像素棋子的硬指标
- [x] 对照确认的重甲步兵概念图重做 v7：分段圆顶盔、侧向护颊、黑铁层甲、双排甲裙、长盾、长枪和破损背旗；红色仅保留在军服、盾牌和旗帜
- [x] v7 接入 ambientCG Fabric 016 与 Leather 026 的 CC0 1K 法线 / 粗糙度贴图，并保留 Metal 010 金属 PBR 表面
- [x] 修复底模双眼继承原素材父级偏移的问题；头脸与被甲胄遮挡的身体分别减面，红兵 14,330 / 黑卒 14,856 三角面并完成正、侧视检查
- [x] 将红兵 / 黑卒 v7 GLB 接入 3D 棋盘并保持离线预加载
- [x] 在 1920×1080 有界面 GPU 浏览器中验收 v7：WebGL2 可用、红黑 GLB 均返回 200、控制台无加载错误，整盘截图保存为 `tmp/pawn-v7-board.png`
- [x] v8 改用 CC BY 的晋代武士人体与贴体层甲，替换程序化拼块躯干；甲片随人体和持械姿态共同变形，不再悬浮在皮肤外
- [x] v8 重做贴头分瓣盔、护颊片、细束盔缨、弧形多层长盾、铆钉、握把和不规则战损，删除玩具式粗盔檐与盾面放射装饰
- [x] v8 将裸手改为深色皮手套材质，枪杆和盾牌收近双手；红黑阵营保留同一装备轮廓，只更换军服、旗帜和盾面颜色
- [x] v8 红兵 / 黑卒均为 8,364 三角面，GLB 规范验证无错误，低于 15k 单角色预算
- [x] 将红兵 / 黑卒 v8 GLB 接入 3D 棋盘并保持离线预加载
- [x] 在 1920×1080 有界面 GPU Chromium 中验收 v8：WebGL2 可用、红黑 GLB 均返回 200、页面无运行错误，整盘截图保存为 `tmp/pawn-v8-board-gpu.png`
- [x] v10f 对照重甲步兵概念图继续精修卒/兵本体：保留完整写实头脸，新增贴头黑铁盔、护颊、盔脊与阵营盔缨；压暗层甲，按阵营重分袖甲/军服，并重做青铜包边三层长盾、膝甲和胫甲
- [x] 删除 v10 迭代中显得像玩具或导致坏网格的巨型战靴、悬空甲裙、球状肩甲、假腰带和自动抽壳手套；完成 v10f 正面三分之四、侧面与近脸回读
- [x] 重新导入红兵 / 黑卒 v10f GLB 验证：各 15,869 三角面、12,281 顶点、23 个网格、13 个材质、1 套 5 骨骼辅助动画架、无空/坏网格，`Idle / Move / Attack / Hit / Death` 五类动作齐全
- [x] 将红兵 / 黑卒 v10f GLB 作为独立版本接入 3D 棋盘并保持离线预加载，保留 v8 便于回退对照
- [x] 在应用内 1920×1080 高画质 WebGL 页面完成 v10f 的 32 子整盘回读：五个红兵与五个黑卒均正常显示，无模型加载或运行错误，截图保存为 `tmp/pawn-v10f-board-1920.png`
- [x] 按用户要求直接采用 HY3 生成的红兵 GLB 作为正式卒/兵造型，不再让旧程序化 v10f 阻塞美术替换；原始红兵约 18,000 三角面并保留其 4K PBR 贴图
- [x] 基于同一 HY3 几何和贴图生成黑卒材质版本：只把红布与红漆替换为蓝黑阵营色，保留皮肤、暗钢、青铜、盾枪和背旗细节
- [x] 将红兵 / 黑卒 HY3 v1 接入 3D 棋盘，按归一化模型尺寸重新校准缩放和脚底落点；在 1920×1080 WebGL 整盘验证十个单位正常显示且无运行错误，截图保存为 `tmp/pawn-hy3-board-1920.png`
- HY3 卒/兵当前确认为静态 GLB；骨架和五类基础动作已迁移至第 12.3 节。
- [x] 直接采用 HY3 红炮 GLB 替换程序化炮：约 25,000 三角面，保留完整炮管、轮式炮架、木铁 PBR、附件与阵营旗；基于同一几何生成蓝黑阵营版本
- [x] 将红炮 / 黑炮 HY3 v1 接入 3D 棋盘并移除旧圆柱占位底座，校准车轮落点、朝向、缩放和标签高度；1920×1080 整盘验证通过，截图保存为 `tmp/cannon-hy3-board-1920.png`
- [x] 将红黑车、马 HY3 v1 接入 3D 棋盘，移除旧几何占位件；按阵营朝向中盘，分别校准双马战车和骑兵长枪的占格、后沿内缩、标签高度及阵营换色
- [x] 将红仕 / 黑士 HY3 v1 双剑重甲护卫接入 3D 棋盘，替换旧几何占位件；校准九宫内朝向、武器占格、模型缩放、标签高度和黑方材质变体
- [x] 将红相 / 黑象 HY3 v1 披甲战象与红帅 / 黑将 HY3 v1 双剑主将接入 3D 棋盘，替换最后两类几何占位件；校准阵营朝向、后沿内缩、相邻棋子间距、主将层级和黑方材质变体
- [x] 用用户新生成的 `xiangqi_king_red` 君王资产替换旧双剑主将：冕冠、龙首肩甲、完整王者披风和坐姿轮廓与士形成明显层级差；将 4K 原始 GLB 优化为 512px 实时版，生成同几何黑将，运行资产升级为 `general-{red,black}-hy3-v2.glb`，并按宽披风占地将缩放校准为 `0.52`
- [x] 修复前排棋子浮空与马朝向错误：HY3 卒脚底、炮轮底本已归零，删除旧组件遗留的 `Y=0.56/0.52` 二次抬升，只保留 `0.01` 防穿面；按马头实际 `+X` 前轴将校正角由 `+90°` 改为 `-90°`，红马朝黑方、黑马朝红方
- [x] 修复“象不能吃子”的假规则限制：象/相吃子规则本身正确，问题来自默认“谱招练习”把所有偏离开局谱的合法着法硬拦截；改为允许合法走子并提示已偏离棋谱，增加红相与黑象不过河吃子的回归测试
- [x] 将七类 HY3 红黑运行资产的颜色、金属粗糙度和法线贴图统一优化为 512px 棋盘实时版本，资源总量由约 494MB 降至约 28MB；完整 32 子同时加载不再触发 WebGL 上下文丢失或自动回退 2D
- HY3 炮当前确认为静态 GLB；车轮、移动、后坐、受击与退场动作已迁移至第 12.3 节。
- [x] 将 `THIRD_PARTY_ASSETS.md` 纳入 Electron 打包文件，确保 CC BY 人物底模署名随安装版分发
- [x] 3D 模式切换为全窗口布局，让棋盘成为主画面，右侧控制区不再挤压棋盘
- [x] 原“逐棋种制作”顺序已被用户提供的七类独立 HY3 模型替代；七类静态模型均已接入并完成整盘回读
- 去除汉字与 UI 后的正式辨识验收已迁移至第 12.4 节。
- [x] 建立并验证 `Idle`、`Move`、`Attack`、`Hit`、`Death` 五个动作剪辑；运行时按阵营前缀选择对应动作
- [x] 在 Blender 中完成第一版比例、朝向、原点、骨骼、动作裁剪和 PBR 材质整理；最终美术精修仍待验收
- [x] 导出单文件 `.glb`，不得在运行时联网加载资产
- [x] 给角色增加统一战场底座、阵营标识和辅助汉字；角色本身仍可凭人形、长枪和长盾识别
- [x] 接入 `useGLTF`、`useAnimations` 和资源预加载，默认播放 `Idle`
- [x] 用 `gltf-transform inspect` 检查并优化 v4：红兵 14,728 三角面、22 meshes / 25 primitives、9 materials、10 animations、约 1.1MB；黑卒同级
- [x] 修复生产版 `file://` 下绝对 `/assets` 路径导致 GLB 加载失败，改为开发版和安装版通用的相对资源路径
- 开发版静态材质、体积和整盘加载已验证；动作与安装包加载验证已迁移至第 12.3、12.5 节。

#### 阶段 C 完成标准

- [x] Blender / GLB / React 静态资产链路已跑通；原“逐棋种后再开始下一个”的闸门已被七类用户资产直接接入方案替代
- 资产再分发权利与 NSIS 安装包验收已迁移至第 12.1、12.5 节。

### 10.4 阶段 D：七种棋子与战斗反馈

- [x] 七种角色轮廓均已替换为用户提供的 HY3 正式静态模型：卒、炮、车、马、士、象、将
- [x] 红黑双方复用同一棋种几何来源，只切换阵营材质；黑方变体由红方模型定向换色生成
- [x] 七类棋子的辅助汉字、阵营环、服装色和角色轮廓已在 1920×1080 战场 / 战术视图中正常显示
- 普通走子、吃子和 `move.capture` 的现状与剩余动作工作统一迁移至第 12.3 节。
- [x] 3D 原型复用 `move.capture` 临时保留被吃棋子，并接通攻方 `Attack`、守方 `Hit/Death`、命中停顿与退场，不新增棋局状态机
- [x] 视觉动作改为独立快照并允许短时并行；最快 120ms 的 AI 应手不会截断上一段死亡演出，也不会反向延迟规则提交或 AI 搜索
- 将军、绝杀与低动态演出已迁移至第 12.3 节。
- [x] 3D 模式响应 `prefers-reduced-motion`：关闭选中摆动、缩短位移，并降低 DPR / 抗锯齿开销

#### 阶段 D 完成标准

- 七类无 UI 辨识、状态回归及动画不阻塞验收已迁移至第 12.2、12.4 节。

### 10.5 阶段 E：性能、回归与发布

- [x] 原“所有棋子统一 15k”预算已被第 11 章按棋种加权预算替代；当前七类基础面数均在新预算内
- 性能记录、资源复用 / LOD、完整回归、安装包和发布工作已迁移至第 12.4、12.5 节。

#### 最终完成标准

- 最终规则、授权、降级和 GitHub Release 验收已迁移至第 12.5 节。

---

## 11. 2026-08-11 资产重审与电影化 3D 呈现对齐（当前最高优先级）

> 参考目标：[alexngdev99/rork-medieval-3d-chess](https://github.com/alexngdev99/rork-medieval-3d-chess)（核对提交 `d9a6be8`）。目标是达到它的“全屏棋盘舞台、角色行军、战斗镜头、灯光氛围和画质分级”完成度，不复制西洋棋规则、三套军队、远程 CDN 资产或 5000 行 `SceneEngine`。本节与旧方案冲突时，以本节为准：默认镜头改为可控透视斜俯视，正交镜头只保留为战术视图；资产预算按棋种加权，不再强制所有单位统一 15k 三角面。

### 11.0 已完成的资产审计与结论

- [x] 检查 `C:/Users/Administrator/Desktop/图片` 的 7 张概念图：均为 1536×1024，可直接作为造型、阵营配色、材质和剪影规范使用
- [x] 检查同目录 14 个红黑 GLB：每个约 29–35 MB、约 49.7–50 万三角面、单网格、单材质、单贴图，无骨骼、无动画、无作者/版权元数据
- [x] 四视图回读确认这些 GLB 把整张设定板中的多个人物视角、旗帜和材质色卡一起重建进一个薄场景，并非独立完整棋子
- [x] 判定 14 个 GLB **不能直接进游戏，也不值得围绕它们做减面和绑定**；保留在桌面作失败样本，不复制到仓库和安装包
- [x] 核对参考仓库许可证：仓库代码为 MIT，可在保留版权与许可文本后选择性改写；其远程托管模型和音频不在本项目复用范围
- [x] 确认可复用思路：`viewport.ts` 镜头适配、`quality.ts` 画质档位、模型归一化/回退、阵营地环+轮廓光、走子/吃子演出节奏
- [x] 确认不需要复用 `gltfQueue.ts`：本项目只离线加载 7 类本地 GLB，Drei `useGLTF` 缓存与预加载已经够用

### 11.1 P0：锁定可合法使用的美术输入

- [x] 用户改为直接提供 `WorkBuddy/2026-08-11-17-02-37` 下七类独立 HY3 红方 GLB；旧的整张概念设定板不再作为运行资产或图生 3D 输入
- [x] 七类输入均为单独、完整、无遮挡的目标单位：步兵、火炮、双马战车、骑兵、双剑护卫、披甲战象、双剑主将
- [x] 运行时风格统一为写实比例、暗钢 / 青铜 / 皮革 / 厚布 PBR，阵营色统一为朱砂红与冷黑蓝
- 生成来源、用户权利确认、SHA-256 和 Release 再分发清单已迁移至第 12.1 节。

#### 11.1 完成标准

- [x] 当前七类 HY3 输入均为单体模型，没有把多视角设定板重建进同一 GLB
- 安装包资产的可核查来源与再分发权利仍是发布闸门，见第 12.1 节。

### 11.2 P0：先做一个参考项目级别的全屏垂直切片

- [x] 保持现有 React Three Fiber `Board3D` 和象棋状态/事件入口，不另建棋局状态机，不搬运参考项目的 `SceneEngine`
- [x] 将 3D 模式改成真正的全窗口舞台：棋盘占据主画面，操作、计时、记录和设置收进边角与可折叠浮层，不遮挡棋盘
- [x] 默认相机改为透视斜俯视，可限制旋转和缩放；从参考仓库 MIT `viewport.ts` 改写“按画面宽高自动求镜头距离/FOV”的算法
- [x] 2026-08-11 重新对齐参考仓库实参：战场镜头采用 46° showcase 方位、焦点抬到棋盘上方 `y=0.35`；战术镜头采用 28° 窄镜头，并按中国象棋 9×10 棋盘外接范围重算距离和缩放边界
- [x] 保留一键战术视图：相机升到近正上方并关闭景深，解决大型车、象、炮遮挡落点的问题
- [x] 七类棋子均已使用正式 HY3 静态模型；在 1920×1080 WebGL 页面完成红黑 32 子战场与战术视图回读
- 主光、环境光、轮廓光、接触阴影和 ACES 色调映射已建立；Bloom、SMAA 与环境雾精修迁移至第 12.4 节。
- [x] 红黑双方同时使用“不同形状的脚下阵营环 + 轮廓光 + 服装色”，不只依赖旗帜或汉字辨认阵营
- 1920×1080 已通过；1366×768、Electron 当前窗口和遮挡点击专项回归迁移至第 12.2 节。

#### 11.2 完成标准

- [x] 1920×1080 全屏首屏已以战场和棋盘为主体，控制 UI 收入边角浮层；参考项目同级光影精修仍见第 12.4 节
- [x] 透视视图可正常下棋，遮挡严重时一键切战术视图继续操作
- 32 子已能稳定同时加载；高低档 FPS 实测与低配机验证迁移至第 12.4 节。

### 11.3 P1：建立正确的单棋种资产流水线

- [x] 当前静态 MVP 改为直接采用用户提供的七类独立 HY3 GLB，不再执行旧概念图高模重拓扑路线
- [x] 红黑双方均从同一棋种红方几何生成，只改变阵营材质；未制作两套不同造型
- [x] 已建立 Blender 批处理脚本：黑方材质变体、纹理优化、GLB 重新导入和结构 / 包围盒 / 面数审计
- [x] 七类当前 GLB 均完成源预览、重新导入、战场整盘和战术俯视回读；没有空网格或模型加载错误
- [x] 七类静态 GLB 已记录顶点、三角面、材质、贴图、动作、包体和包围盒；加载时间与运行内存迁移至第 12.4 节
- [x] 原按棋种顺序制作的限制已由用户一次性提供七类可用独立模型取代
- 当前 GLB 均无骨骼和动画；统一绑定与 `Idle / Move / Attack / Hit / Death` 动作迁移至第 12.3 节。

#### 运行时预算（先按此执行，实测后再调）

- [x] 面数预算通过：卒 18k、士约 18k、将约 24.1k 三角面
- [x] 面数预算通过：马 34k、炮 25k、车 48k、象约 44.9k 三角面；中低档 LOD 仍见第 12.4 节
- [x] 初始 32 子约 907,636 三角面，低于约 100 万目标；同阵营同棋种由 `useGLTF` 缓存并共享几何 / 贴图
- [x] 七类红黑 PBR 运行贴图统一优化为 512px，14 个 GLB 总量约 28MB；保留颜色、金属 / 粗糙度与法线通道

### 11.4 P1：对齐参考项目的走子与战斗呈现

- [x] 第一阶段已接通卒/兵 GLB 的 `Move` 动作：移动时始终播放、低画质也不关闭；其余占位棋种改为贴地移动，不再统一跳弧，且仍沿用现有 260ms 棋局动画时钟
- [x] 卒/兵吃子垂直切片已接通：骨骼安全独立克隆 → 攻方 `Attack` → 命中环/FOV 轻推 → 约 120ms hit-stop → 守方 `Hit/Death` → 攻方落位；其余棋种暂用同一时序的占位退场
- [x] 修复全屏 3D 的父级 flex 高度塌陷；1366×900 硬件 Chrome 中画布由 `1350×0` 恢复为 `1350×836`
- 七类静态模型的转向、基础位移、命中环、短 hit-stop、退场和 FOV 轻推已经接通；骨骼动作、棋种专用攻击、音效 / 尘土、将军 / 绝杀演出及电影镜头开关统一迁移至第 12.3 节。
- [x] 动画层只消费现有 `moveAnimation/captureBurst/checkmateMove`，不改变规则、历史、AI 搜索或走法提交时机

### 11.5 P1：画质档位、后处理与稳定降级

- [x] 已实现 `低 / 中 / 高 / 极高` 四档并真实控制 DPR、抗锯齿、阴影尺寸、接触阴影和待机动作开关
- [x] 首次启动按 GPU/CPU/内存粗选档位，再用实际帧时间自动逐级降低；只降一级并留出观察窗口，避免画质来回跳
- [x] 低档：无后处理、DPR 1、简化阴影、关闭所有待机骨骼；走子/攻击/死亡动作仍必须保留
- [x] 已启用 ACES 色调映射、分档抗锯齿和接触阴影；Bloom、SMAA 与短时景深迁移至第 12.4 节
- [x] 处理 WebGL context loss；恢复失败或持续低于 30 FPS 时沿用现有路径切回完整可玩的 2D 棋盘
- [x] 所有模型、贴图、音效离线随安装包分发，不依赖参考仓库的 R2 CDN

### 11.6 P2：选择性复用参考仓库代码时的边界

- [x] 只改写小而独立的算法：镜头适配、质量配置、模型测量/落地/缩放、阵营环与轮廓光；优先落到现有 `Board3D`，不新建平行渲染框架
- [x] 不复制国际象棋专用的 8×8 棋盘、升变、王车易位、三套军队、法术/火枪分支、AI 引擎和 UI 组件库
- [x] 若实际复制了 MIT 代码片段，在仓库和安装包中保留 `Copyright (c) 2026 King's Gambit contributors` 与 MIT 许可文本，并在 `THIRD_PARTY_ASSETS.md` 标明文件与改动
- [x] 不直接下载参考项目远程模型和音频；除非后续拿到单独、明确、可再分发的资产授权

### 11.7 最终验收

- 最终辨识、色觉、功能、安装包、截图和 Release 验收已统一迁移至第 12 章，避免与历史清单重复维护。

---

## 12. 2026-08-12 当前唯一执行清单

> 本章是当前唯一有效的未完成任务入口。第 10、11 章保留为实施历史；其中尚未完成的有效事项已经全部迁移到这里，不再重复勾选旧条目。当前代码实施从第 12.2.1 节开始；第 12.1 节为可并行整理、但发布前必须通过的权利闸门。

### 12.0 当前基线（已完成）

- [x] 七类棋子共 14 个红黑 HY3 GLB 已全部接入：卒、炮、车、马、士、象、将；旧程序化棋子只保留为技术历史，不再作为当前 3D 主画面
- [x] 红黑双方使用同棋种同几何来源，黑方材质由红方模型定向换色生成；皮肤、金属、皮革和中性色不随阵营误变色
- [x] 14 个运行 GLB 的 PBR 贴图统一优化为 512px，总包体约 28MB；完整 32 子可同时加载，不再因显存压力触发 WebGL context loss
- [x] 当前模型面数符合加权预算；初始 32 子约 907,636 三角面，低于约 100 万目标
- [x] 46° 战场透视镜头、28° 战术俯视镜头、9×10 棋盘自适应构图、全屏布局和边角 HUD 已完成
- [x] 开发版已完成 1920×1080 战场 / 战术整盘回读，模型与材质均无加载错误；原“棋盘贴面、标签与朝向统一”视觉结论已按用户反馈撤回，重新列入第 12.2.1 节
- [x] `低 / 中 / 高 / 极高` 画质档、帧时间自动降档、WebGL context loss 处理和完整 2D 回退路径已接通
- [x] 2026-08-12 已通过 `npm run build`、`npm run selftest`、GLB 重新导入审计和 `git diff --check`

### 12.1 P0：资产权利与发布清单

- [x] 记录七类 HY3 模型的生成工具全名与版本、生成日期、生成账号 / 所有人、输入来源以及当时适用的服务条款：已从最新 WorkBuddy 会话记忆、应用日志和产物时间确认腾讯混元 3D 文生 3D（model 3.1 + PBR）、2026-08-11 至 2026-08-12、本机已登录的项目所有人会话及自写逐棋种文本提示词；官方条款链接和第 4.3 条权利边界已写入清单，账号 UUID / 凭据不进入公开包
- [x] 2026-08-12 项目所有人已明确确认并授权：本项目使用的腾讯混元 3D 生成模型、随附贴图及项目制作的黑方衍生版本，可以商业使用、修改，并可随 GitHub Release 与 Windows 安装包再分发；该确认覆盖当前运行清单中的 14 个 HY3 GLB。仍需遵守适用法律、腾讯服务条款以及资产清单中记录的第三方输入权利边界。
- [x] 为 14 个最终 GLB 生成 SHA-256 清单，记录棋种、阵营、三角面、贴图尺寸、包体和生成链路；提供 `npm run audit:assets` 可重复生成 Markdown 与安装包内 JSON，最终将 / 帅只统计 v2 君王资产
- [x] 更新 `THIRD_PARTY_ASSETS.md`，区分用户生成红方资产、项目自制黑方变体、第三方代码与 CC0 / CC BY 材质；明确权利确认前清单不代表已取得再分发权
- [x] 确保许可证、署名和资产清单进入 NSIS 安装包：`THIRD_PARTY_ASSETS.md`、`HY3_ASSET_MANIFEST.md`、`licenses/**` 随应用打包，机器清单随 `dist/assets` 进入运行资源；`build:installer` 构建前强制重跑资产审计。权利未确认前仍不发布包含这些 GLB 的公开 Release
- [x] 收紧安装包 3D 资产白名单：生产构建读取机器清单，仅允许当前代码实际使用的 14 个 HY3 GLB 进入 `dist/assets`；旧卒草稿、旧将 v1 和历次程序化试制文件仍保留在工作区，但不会混入 NSIS 安装包

### 12.2 P0：棋盘结构、统一朝向与完整对局回归

> 2026-08-12 已只读对照参考仓库的 [`board.ts`](https://github.com/alexngdev99/rork-medieval-3d-chess/blob/main/web/src/scene/board.ts) 与 [`pieces.ts`](https://github.com/alexngdev99/rork-medieval-3d-chess/blob/main/web/src/scene/pieces.ts)：参考项目用统一 `BOARD_TOP / TILE / squareToWorld` 管理棋盘、棋子、点击与特效坐标；棋盘是有厚度、有倒角、有边框的完整物件，贴面特效只使用受控的毫米级高度差；棋子采用 `placement container → runtime → visual` 三层结构，以模型前轴校正、阵营 `homeFacing` 和移动后回正统一朝向。这里只复用这些工程方法，不能照搬其 8×8 国际象棋格局、模型或玩法。

#### 12.2.1 P0：先重做棋盘落面与棋子朝向

> 当前问题已定位：`BoardLines` 是整体放在棋面上方的独立细盒；河界底色是另一张悬空透明平面；“楚河 / 汉界”和 32 枚棋子的名称使用始终朝向屏幕的 `<Html>`；不同棋种又分别使用 ±90°、0°、180° 的临时旋转，走子时外层节点还会叠加一次朝向。以下项目全部完成前，不再把当前棋盘与朝向视为通过验收。

- [x] 建立唯一棋盘坐标契约：集中定义 `BOARD_TOP`、交点间距、9×10 边界、棋盘外沿和 `boardToWorld / worldToBoard`；棋盘、交点点击体、棋子、阴影、高亮、河界和镜头目标均只从这套坐标生成
- [x] 取消现在“棋子坐标随 `flipped` 旋转一次、镜头又旋转一次”的双重翻转；世界棋局坐标保持稳定，红黑换边只旋转镜头及其屏幕映射，点击命中通过统一逆变换还原到逻辑坐标
- [x] 按参考项目的物件层级重构成一块完整棋盘：厚实底座、倒角外框、窄金属 / 深木收边和单一 PBR 棋面；清除当前三个互相割裂的矩形层及悬空半透明河界贴片造成的“搭积木”观感
- [x] 严格重画中国象棋盘：10 条横线；左右两条纵线贯通；中间 7 条纵线在楚河汉界处断开；上下九宫各两条对角线；所有端点必须精确落在 90 个交点中心，线宽、边距和对称性一致
- [x] 将格线、九宫线、楚河汉界和必要纹样合成到同一张棋面 UV / Canvas 贴图（优先）或真正的浅刻槽；禁止继续用悬在棋面上方的 `boxGeometry` 细条拼线
- [x] 将“楚河 / 汉界”烘焙为棋面内的字样或贴面 decal，跟随棋盘透视、遮挡和光照，不再使用会朝向相机的 `<Html>`；低角度、缩放和旋转镜头时不得产生视差或漂浮
- [x] 默认移除每枚棋子头顶的永久 `<Html>` 名称胶囊；七类模型本身承担辨识，若保留辅助名称，只允许作为默认关闭的战术 / 无障碍开关，且不得污染主战场画面
- [x] 为所有贴面层建立固定高度与深度规则：棋面文字 / 线、接触阴影、阵营环、高亮分别使用统一微偏移，并明确 `depthTest`、`depthWrite`、`polygonOffset` 与 `renderOrder`；任何视角下都不得穿插、闪烁或看出悬空间隙
- [x] 为 14 个 HY3 GLB 做前轴审计，逐个记录局部 `frontAxis`、`upAxis`、脚底 `baseY`、中心、缩放和导入校正；优先在资产处理阶段统一为 `+Y` 朝上、同一局部前轴，红黑同棋种不得有不同轴约定
- [x] 将棋子节点统一为 `placementRoot（只负责棋盘位置）→ runtimeRoot（只负责转向 / 动作）→ visualRoot（只负责模型缩放、居中和一次性轴校正）`；删除马 / 车 / 象与士 / 将两套互相矛盾的运行时硬编码角度
- [x] 用四元数或统一 `atan2` 约定生成阵营 `homeFacing`：静止时红方七类全部朝向黑方、黑方七类全部朝向红方；移动 / 攻击只临时面向目标，落位后按参考项目平滑回到本阵营朝向
- [x] 增加仅开发环境可见的“14 模型朝向校准排布”：显示世界前向箭头、脚底平面、包围盒和棋盘行进方向；同屏确认卒、炮、车、马、士、象、将的红黑版本，无需靠猜角度调模型
- [x] 固定输出 1920×1080 与 1366×768 的战场 / 战术、红方 / 黑方视角截图；验收棋盘占画面比例、边框厚度、90 个交点、河界、九宫、文字落面和 32 子朝向，并与参考项目的完整物件感和视觉层级并排对照
- [x] 最终动态验收：新增 `npm run capture:3d-dynamic-qa`，在 Electron 硬件 WebGL 中依次执行初始局面、Canvas 视觉交点像素点击普通走子、双方走子、吃子、红黑翻转、俯视、电影、席位环绕 / 缩放、1366×768 窗口缩放、悔棋与重开，共输出 11 张步骤图和 JSON；2026-08-12 由 AMD Radeon RX 6500 XT / ANGLE D3D11 连续通过。逐图确认棋面线字随透视稳定、32→31→32 子状态正确、移动 / 翻转后回到阵营朝向、点击投影落在统一 `boardToWorld / worldToBoard` 交点。验收同时发现并移除会在斜视与缩放时露出巨大白色矩形边界的 `ContactShadows` 平面，保留方向光真实落地阴影。

#### 12.2.2 P0：可完整下棋与响应式回归

- [x] 在 3D 模式从初始局面完整下完一局：新增 `npm run capture:3d-fullgame-qa`，由现有规则引擎生成并固化一条从标准初始局面出发的 39 手合法终局路线，Electron 全程通过真实 UI 选择 / 落子，不切回 2D；AMD Radeon RX 6500 XT 硬件回放最终得到“红方胜利，黑方被将死”，历史严格为 39 手，并输出第 10 / 20 / 30 / 39 手四张中途画面确认模型与吃子状态同步。另在 12 步动态 QA 中重开后执行谱招“炮二平五”，等待并确认 AI 自动完成第 2 手，应手链路正常。将军 / 绝杀弹字的专项截图仍并入下面反馈语法回归，不把本项扩大解释为所有特效已验收。
- [ ] 战场 / 战术两个视角分别验证点击、选中、合法点、最后一步、提示、吃子目标和绝杀高亮；再对红黑翻转各测一次
- [x] 修复连续快速走子、连续吃子、AI 立即应手、悔棋和重开的瞬态残留竞态：每次历史变化先同步清除旧移动、落地与吃子状态，再按前进 / 一至两步悔棋重建动画；跨多步重开不播放伪悔棋；3D 动画队列在上游动画失效时立即清空，并增加普通走子、吃子、单步/整回合悔棋和重开分类自测
- [x] 将 3D 点击从 32 个 HY3 可视网格解耦为唯一棋盘拾取平面：大型车、象、马及武器不再截获射线，任意镜头下都用命中世界点经 `worldToBoard` 还原交点；已通过 1920×1080 构建与坐标自测，1366×768 / Electron 人工点按继续并入最终动态验收
- [ ] 回归新局、换边、翻转、提示、复盘、自动陪练、AI 思考和桌宠小窗，确认 3D 渲染不改变规则、AI 棋力或提交时机

### 12.3 P1：逐棋种骨骼、动作与战斗演出

> 当前 14 个 HY3 GLB 均为静态模型、无骨骼、无动画。按下面顺序逐项完成；每一项必须重新导入 GLB 并在整盘中验收后再勾选。

- [ ] 卒 / 兵：绑定人形骨架并补齐 `Idle / Move / Attack / Hit / Death`
- [ ] 士 / 仕与将 / 帅：复用人形骨架和基础动作，分别制作护卫双剑与主将双剑演出
- [ ] 马：绑定骑手与坐骑，制作待机、行进、跃步攻击、受击和死亡，不使用人形滑行动作
- [ ] 炮：绑定车轮 / 炮架，制作移动、瞄准、后坐、命中和退场；炮弹命中后再提交视觉落位
- [ ] 车：绑定双马与战车，制作车轮、牵引、冲撞攻击、受击和退场
- [ ] 象 / 相：绑定战象，制作踏步、冲撞 / 象鼻攻击、受击和倒地
- [ ] 普通走子统一实现“转向 → 按棋种步频播放 Move → 位移 → 落地回正”，脚步声、尘土和落地反馈共用移动时钟
- [ ] 吃子统一实现“镜头轻推 → Attack → 命中火花 / 短 hit-stop → Hit / Death → 消散 → 攻方落位”，同时保留被吃模型至动作结束
- [ ] 将军增加短促边缘光、主将顶光和轻震屏；绝杀增加受击与短暂停顿，不持续遮挡盘面
- [ ] 增加“吃子电影镜头”开关；`prefers-reduced-motion` 和低性能档关闭推镜、震屏与大粒子，但保留必要走子 / 攻击状态
- [ ] 验证所有视觉演出只消费现有事件，不延迟棋局提交、AI 搜索或用户下一次有效操作

### 12.4 P1：画面、LOD 与性能验收

- [ ] 精修主光、环境光、冷暖轮廓光和环境雾；中高档按需加入 Bloom、SMAA，景深只在短时电影镜头启用
- [ ] 为马、炮、车、象制作约 50% 面数的中低档 LOD；检查重复棋子继续共享几何、材质和贴图缓存
- [ ] 记录开发机在 1920×1080 四档画质下的 FPS、首屏加载时间、GPU / 系统内存和安装包体积
- [ ] 至少使用一台低配机器验证：高档目标 60 FPS，低档不低于 30 FPS；不足时按 DPR → 阴影 → 动画 → 2D 顺序降级
- [ ] 去掉汉字、旗帜和 UI 后验收七类棋种远景辨识；在暗场、近远景和色觉异常模拟下验证红黑阵营可区分
- [ ] 用固定 1920×1080 截图清单对照参考项目验收全屏占比、镜头、光影、材质、角色比例、走子节奏、吃子镜头与 UI 避让

#### 12.4.1 P0：参考成熟项目重做战场主画面

- [x] 主动拆解参考项目的 `viewport / environment / battlefield / arena / textures / postfx / quality`，分别记录镜头、地形、建筑剪影、材质调色、后处理和画质裁剪方法；只复用工程方法与自制程序化几何，不下载其远程模型资产
- [x] 默认战场镜头改为本方军阵后方的居中玩家席位；针对 HY3 单位远高于参考棋子的实际比例，抬高并后退镜头，避免斜向菱形构图和近排遮盘
- [x] 移除黑色空地板与红蓝舞台补光，加入泥土地形、旧石台基、双侧残墙、远山天际线、营帐、阵旗、拒马、碎石和火盆，场景四周不再是纯黑空洞
- [x] 环境接入画质分档：低档只保留地形、台基、残墙和远景剪影；中档增加营帐、军旗与战场道具；高/极高档再启用火光，避免装饰无条件挤占 32 枚 HY3 棋子的显存预算
- [x] 棋盘从高饱和橙金改为低饱和旧石、烟熏土色与氧化青铜，棋面加入确定性磨损、污迹和裂纹；同步重调雾色、主光、冷色填光和相机补光
- [x] 用硬件 WebGL 固定输出 1920×1080 的红方/黑方战场视角与战术视角：新增 Electron 应用内 `capturePage` QA 模式和 `npm run capture:3d-qa`，强制视角、阵营、主题和高画质，输出 PNG + GPU JSON；2026-08-12 四图均由 AMD Radeon RX 6500 XT / ANGLE D3D11 渲染，不是 SwiftShader，截图位于 `tmp/qa-*-1920.png`。已逐张确认棋盘与 32 子完整、线字贴面、红黑视角构图稳定；战术俯视辨识度和周边低模建筑美术仍归下一条继续精修
- [ ] 将当前程序化远山、残墙和营帐继续替换或精修为有中国古战场结构特征的低模资产；优先寻找可再分发的统一场景包，许可证和来源写入 `THIRD_PARTY_ASSETS.md`
- [x] 参考项目的完整主题重照明方法增加“阴天战场 / 黄昏烽火”两套统一环境主题：天候选择会持久化，并成套切换背景、雾、泥地、远山、残墙、营帐、棋盘、主光、冷暖填光和相机补光，不是简单亮度滑杆；画质分档和火盆数量预算保持独立

#### 12.4.2 P0 / P1：第二轮完整运行与源码审计后的增量清单

> 2026-08-12 已将参考仓库完整克隆到 `D:/reference-projects/rork-medieval-3d-chess`，审计提交 `a3c1580e1ac9be51348306c56017f14405b299d9`。已实际安装依赖、通过生产构建并在本机 Chrome 启动；核心 Vitest 3 个文件共 22 项通过，浏览器测试阶段因本机缺少 Playwright Chromium 包而未执行，不能记作浏览器测试通过。已实看出征菜单、默认 3D、战术 2D、镜头菜单、设置面板和 `390×844` 窄屏。对照截图位于 `tmp/reference-rork-*.png`，仅作本地 QA 证据，不进入安装包。
>
> 审计结论：参考项目最值得学习的不是西洋棋造型，而是“主战场始终居中、3D 与战术可读层分工、镜头数学适配、视觉事件编排、显卡自检与安全渲染、主题化调色/音频、分档真实裁剪”这一整套产品化方法。其代码为 MIT；源码中同时存在约 151 次远程 `.glb`、35 次 `.mp3` 和 9 次 `.png` 引用，均指向 Rork R2。仓库 MIT 许可不能自动证明这些 CDN 资产可再分发，所以仍只学工程方法，绝不把远程模型、图片或音频打进本项目。

##### P0：先补齐直接影响“能看清、能下棋、不会黑屏”的能力

- [x] 将“战术视图”从“复杂 HY3 模型的正上方俯拍”改成独立可读层：战术模式隐藏人物模型，改用贴地军令牌并以规范汉字显示七类棋种；红方采用朱砂圆牌、黑方采用冷青八角牌，形状、底色、纹样和文字同时区分，不只靠颜色。静止、走子、吃子退场、选中脉冲与红黑翻转均复用同一棋盘坐标、动画和透明拾取平面；退出后恢复 3D 模型。2026-08-12 已通过 `npm run selftest`、`npm run build` 和 AMD Radeon RX 6500 XT 硬件 Electron 四图回读；`qa-red-tactical-overcast-1920.png` 与 `qa-black-tactical-dusk-1920.png` 中 32 子完整、汉字朝向玩家且无模型遮点。1366×768 人工点按仍并入第 12.2.1 节最终动态验收。
- [x] 建立完整但克制的镜头预设：`红方席位 / 黑方席位 / 俯视 / 电影 / 战术 / 翻转 180°` 已接入同一 HUD；红黑席位和 180° 命令直接同步应用原有 `flipped` 状态，不存在第二套方向状态。席位镜头居中、俯视保留 HY3 人物、电影镜头采用受约束斜角、战术镜头切军令牌；全部按 9×10 外接范围、实际模型高度、画面宽高和当前阵营重新求 FOV / 距离 / 焦点，采用短平滑过渡，`prefers-reduced-motion` 下立即落位。默认不自动逐回合旋转。2026-08-12 已用 1920×1080 AMD RX 6500 XT 输出红方席位、黑方席位、红方俯视、黑方电影和红黑战术共六张固定图，棋盘与 32 子均完整；又以 1920×1080、1366×768、900×900、430×620 对四类镜头执行数学边界自测。连续点击切镜、切镜中翻转和 reduced-motion 人工观感继续并入第 12.2.1 节最终动态验收。
- [x] 把镜头适配扩展为明确的宽高比验收矩阵：2560×1440、1920×1080、1366×768、900×900、430×620 均进入镜头求解自测；每个预设都会校验垂直 / 水平投影视锥至少容纳棋盘、最高模型、长兵器和特效所需的 `6.9` 外接安全半径。席位镜头限制在本方后场约 55° 水平扇区、电影镜头约 34°，最近距离不进入人物、最远距离不退进两侧残墙，最大俯仰不允许棋盘压成地平线；俯视 / 战术镜头锁定旋转。Electron QA 已支持 `--qa-width / --qa-height` 并记录真实 viewport / canvas；2026-08-12 用 AMD RX 6500 XT 实拍 1366×768 席位、900×900 电影与最小 430×620 战术窗口，棋盘及 32 子完整，最小窗口 HUD 在底部换行且未遮底排。环绕 / 缩放极值的鼠标人工操作继续并入第 12.2.1 节最终动态验收。
- [ ] 重排全屏 HUD 的信息层级：左侧只放轮次 / 阵营 / 计时，顶部只放高频命令，右侧为可折叠的战绩 / 吃子 / 走法记录；棋盘视觉中心不得因侧栏开关移动。宽度不足时右侧内容进入抽屉而不是覆盖棋盘。参考项目 `390×844` 下镜头菜单、Ledger 和 Spoils 会互相遮挡，这个缺陷明确不照搬。
- [ ] 统一棋盘反馈语法：选中、合法落点、可吃目标、最后一步、提示、将军、绝杀和预走使用固定且彼此不同的形状 / 色彩 / 节奏；选中时可轻度压暗不可达区域，吃子目标必须比普通落点更醒目。涟漪、格面轻震、落尘只作质量分档后的瞬时反馈，不得抬高棋盘线字或污染点击平面。
- [ ] 增加运行时 GPU 诊断与“安全渲染”开关：显示实际 renderer、WebGL 版本、当前 FPS 和当前画质；识别 SwiftShader / llvmpipe / software renderer、浮点过滤和半浮点渲染能力。启动后用极小离屏目标做一次环境光 / 反射回读，检测到黑帧、NaN、连续渲染异常或后处理异常时立即关闭后处理、环境反射和阴影，保留直接渲染；安全模式可由用户手动开关且原因可见。
- [ ] 给后处理增加“直接渲染兜底”：低档始终绕过 composer；中高档按能力启用调色、SMAA、Bloom，SSAO 只在极高档，景深只在短时吃子 / 绝杀镜头。后处理初始化或任一帧连续失败时只降级画面，不得让棋盘黑屏、卡死或切断棋局。
- [ ] 将 3D 演出接入带版本号 / 取消令牌的视觉事务：每次走子、吃子、悔棋、重开、切视图或卸载都会让旧异步片段失效；攻击者、受击者、镜头、音效和粒子按同一时间线编排，被吃模型保留至受击 / 死亡完成。视觉事务只消费既有棋局事件，任何素材失败都走占位退场，绝不回滚或延迟规则提交、AI 搜索与下一次有效输入。
- [ ] 新增发布前黑屏与降级专项：分别模拟后处理抛错、环境贴图无效、WebGL context lost、静态模型加载失败和 20 FPS 持续低帧；验收安全模式、逐级降档和完整 2D 回退均可恢复一盘棋，并将 GPU 报告、触发原因和最终路径写入 QA JSON。

##### P1：补齐“像成熟游戏”的动作、声音、加载和画质体系

- [ ] 为第 12.3 节的每个棋种建立显式资产契约：模型本体、局部前轴 / 上轴、脚底、目标高度、骨架兼容组、`Idle / Move / Attack / Hit / Death` 片段、循环方式、混合时长、命中时刻和步态周期都写入元数据；加载时一次完成测量、居中、落地和轴校正，运行时不再散落棋种特判。
- [ ] 动画位移按片段真实步频同步，而不是统一匀速滑行：可从骨骼关键帧估算步态周期，脚步声和落尘落在接触帧；攻击按棋种记录 impact time，受击 / 死亡交叉淡化。相同人形骨架的卒、士、将共享兼容动作，马、象、炮、车保留独立绑定与运动规律。
- [ ] 当第 12.3 节开始加入分离动作 GLB 后，再增加全局解码 / 预热队列，桌面并发上限 4、低配上限 2，并带进度、有限重试和棋种级回退；首屏只阻塞默认镜头可见的模型和必要动作，其余攻击 / 死亡片段后台预热。当前 14 个本地静态 GLB 继续用 `useGLTF` 缓存，不为不存在的网络问题提前搬入参考项目整套下载器。
- [ ] 将四档画质扩展成真正控制成本的统一配置：DPR、阴影、阴影图、SMAA、Bloom、SSAO、景深、待机骨骼、尘土 / 火星 / 烟雾数量、火盆动态光、远处军阵和战场道具都由同一档位表驱动；移动、攻击和死亡动作在所有档位保留。切档后必须释放被关闭的渲染目标、粒子和灯光资源。
- [ ] 建立“清晰模式”和“电影模式”的独立调色参数：常态下压低颗粒、暗角、Bloom 和景深，优先保证棋盘线、落点与阵营可读；吃子 / 绝杀短镜头才临时加强暖高光、冷阴影、镜头冲击和景深。阴天与黄昏主题分别拥有自己的雾、曝光、调色、Bloom 阈值和环境音，不只换地面颜色。
- [ ] 建立主题化音频总线：`音乐 / 环境 / 棋子动作 / UI / 人声` 可分别限幅与总开关；阴天和黄昏环境声可交叉淡入，重击时短暂 duck 音乐 / 环境，脚步、兵器、炮击和落地按世界位置做轻量左右声像。设置同时发声上限并复用噪声 / 音源缓存；所有音频必须本地离线且许可可核查。
- [ ] 增加可选的一步预走以改善 AI 思考期间的操作感：只允许排队 1 步，基于预计轮到用户时的局面复核合法性；被将军、AI 落子导致非法、悔棋、重开或按 Esc 时立即取消，并在棋盘上显示独立路径。不得为了展示动画增加 AI 最短思考时间，也不首版照搬 3 / 5 步连锁预走。
- [ ] 将“AI 最短思考延迟”明确排除：参考项目设置中的 `0.4 / 1.5 / 3 / 6 秒`人为等待不进入本项目；Pikafish 一旦完成当前强度搜索就立即应手，视觉演出与思考并行，不能用延迟伪装节奏，也不能以降深度换速度。
- [ ] 增加性能生命周期管理：页面不可见 / 窗口最小化时暂停非必要帧循环和环境粒子，恢复时重置 delta；粒子、命中环和弹道使用对象池，逐帧路径避免临时分配；场景 / 主题 / 3D 模式卸载后检查几何、材质、纹理、RenderTarget、AudioNode 和事件监听均已释放。
- [ ] 设置页增加“自动检测结果 + 实时 FPS + 手动画质 + 亮度 + 安全渲染 + 吃子镜头 + 减少动态 + 辅助棋徽 + 声音”完整状态；自动降档只能逐级向下且说明原因，用户手动选档后本次会话不再偷偷升降。
- [ ] 继续使用固定截图和硬件 GPU 回读做视觉回归，同时补充：战术 token、六个镜头命令、HUD 展开 / 折叠、所有高亮状态、两套主题、清晰 / 电影模式和安全渲染；截图不得只拍初始局面，至少包含一次普通走子、一次炮隔子吃、一次象吃子、一次将军和一次绝杀。

##### P2：完善完整产品感，但不阻塞当前棋盘与动画主线

- [ ] 为进入“全屏 3D 对局”增加轻量出征页：在同一实时战场背景上选择红 / 黑方、AI 难度、先后手、主题和画质，再进入棋局；桌宠启动和 2D 快速开局保持原路径，不强迫每次经过大型菜单。
- [ ] 将走法记录与吃子统计做成默认折叠的战报抽屉，支持复制棋谱、悔棋、认输、重开和复盘定位；任何展开状态都不得遮住棋盘主要落子区，计时和当前行棋方始终可见。
- [ ] 可选“棋种徽记 / 汉字军牌”只在战术视图或无障碍开关开启时显示，并随镜头保持可读；默认 3D 战场仍由模型轮廓、武器、坐骑和服装层级承担七类辨识，不能重新依赖永久悬浮姓名标签。
- [ ] 检查 3D 运行时主包体和首屏加载瀑布，按需懒加载全屏战场、设置面板、电影后处理和非首屏动作；为主线程脚本、首屏 GLB 解码和峰值显存设预算，避免参考项目约 1.53MB 单块主 JS 和超大场景引擎继续膨胀的做法。

##### 明确不照搬的参考项目内容

- [x] 不复制 8×8 西洋棋规则、升变 / 王车易位、三套西式军队、法术 / 火枪玩法、英文中世纪纹章和远程 R2 资产；只将可独立验证的小算法改写进现有 React Three Fiber 架构。
- [x] 不复制其 5000 余行单体 `SceneEngine`；镜头、视觉事务、后处理、音频、资产契约和诊断继续保持模块化，并通过现有棋局事件连接。
- [x] 不照搬其窄屏 HUD 遮挡、每回合自动转镜头、长时间电影演出、多步预走和人为 AI 思考下限；这些功能只吸收目标，不继承对中国象棋可读性、响应速度和桌面应用形态不合适的实现。

### 12.5 P2：安装包、回归与 Release

- [x] 2026-08-12 依次通过 `npm run selftest`、`npm run build`、`npm run benchmark:engine` 和 `npm run build:installer`；引擎 6 个局面最佳着全部一致，优化后平均 1400ms / 深度 18.83；NSIS x64 完整生成
- [ ] 在 NSIS 安装版完成 WebGL 创建、模型 / 材质 / 动画加载、启动、完整对局和退出烟雾测试
- [ ] 检查首次安装、覆盖升级、卸载和完全离线启动；强制 WebGL 失败时 2D 仍可完整下棋
- [ ] 核对安装包内模型、贴图、音效、Pikafish、许可证、署名和资产清单均齐全，运行时不访问外部 CDN
- [ ] 发布 `v1.3.0-alpha`，GitHub Release 必须附可直接安装的 Windows 成品、变更说明和已知限制，不只上传源码压缩包
- [ ] 收集并修复 alpha 阻塞问题，不趁机增加新功能
- [ ] 发布稳定版 `v1.3.0`，附安装程序、资产许可、性能记录、完整变更说明和 2D 降级说明
